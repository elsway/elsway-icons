Autonaut Icons — vehicleinfo
========================================

2804 glyphs — every icon in both weights, in one font.
Generated from the SVG source; do not hand-edit.

Files
  autonaut-vehicleinfo.ttf       the icon font
  autonaut-vehicleinfo.woff      same font, web-optimised
  autonaut-vehicleinfo.css       helper classes
  codepoints.json       every glyph name and its codepoint

Naming
  Each glyph is "<icon>-<weight>", so both weights sit side by side:
    <i class="ai-heart-regular"></i>
    <i class="ai-heart-fill"></i>

Web
  1. Copy the font files and the CSS into your project.
  2. <link rel="stylesheet" href="autonaut-vehicleinfo.css">
  3. <i class="ai-heart-fill"></i>

Anywhere else (Figma, Sketch, native apps)
  Install autonaut-vehicleinfo.ttf and type the glyph's codepoint.
  Codepoints start at U+E900 and are listed in codepoints.json.

Colour and size follow the surrounding text, exactly like a letter:
  .my-icon { font-size: 32px; color: #1a1a1a; }
