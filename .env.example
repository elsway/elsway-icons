# Elsway Icons — client env
# Copy to .env.local for local dev, or add to Vercel env vars for production.
# Leave blank to keep the CMS disabled (site still works read-only).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
